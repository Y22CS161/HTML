<!DOCTYPE html>
<html>
<head>
    <title>Bio-data Form</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }

        form {
            max-width: 500px;
            margin: auto;
        }

        label {
            display: block;
            margin-top: 10px;
        }

        input, select, textarea {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            margin-bottom: 10px;
            box-sizing: border-box;
        }

        button {
            padding: 10px;
            background-color: #4CAF50;
            color: white;
            border: none;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>

    <form>
        <label for="fullName"><b>Full Name:</b></label>
        <input type="text" id="fullName" name="fullName" required>

        <label for="dob"><b>Date of Birth:</b></label>
        <input type="date" id="dob" name="dob" required>

        <label for="gender"><b>Gender:</b></label>
        <select id="gender" name="gender" required>
            <option value="male">Male</option>
            <option value="female">Female</option>
            <option value="other">Other</option>
        </select>

        <label for="address"><b>Address:</b></label>
        <textarea id="address" name="address" rows="4" required></textarea>

        <label for="email"><b>Email:</b></label>
        <input type="email" id="email" name="email" required>

        <label for="phone"><b>Phone:</b></label>
        <input type="tel" id="phone" name="phone" pattern="[0-9]{10}" required>

        <label for="education"><b>Qualification:</b></label>
        <input type="text" id="education" name="education" required>

        <label for="file"><b>Resume (PDF only):</b></label>
        <input type="file" id="file" name="file" accept=".pdf" required>

        <label for="color"><b>Favorite Color:</b></label>
        <input type="color" id="color" name="color" value="#ff0000">

        <label for="range"><b>Experience Level (0-10):</b></label>
        <input type="range" id="range" name="range" min="0" max="10" value="5">


        <button type="submit">Submit</button>
    </form>

</body>
</html>
